package test.problems

import problems.binarytree._
import org.specs2.mutable._

class BinaryTreesSpec extends Specification {
  "example1" should {
    "returns XXX if (a, b) = (0, 1)" in {
      pending
    }
  }
}
